<!doctype html>
<html lang="en">
  <head>
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        Welcome to D-Shave
      {{/if}}
    </title>
    {{> styles}}
  </head>
  <body class="services booknow">
    {{> nav }}
    <div class="container-fluid">
      <div class="row row-no-padding">
        <div class="col-sm-12">
          <div class="barber-header">
            <img src="assets/img/booknow-header.jpg" alt="Barber Shop">
            <div class="barher-inner">
              <h1>book now</h1>              
            </div>
          </div>
        </div>
      </div>
    </div> 
    
    {{> booknow }}
    {{> footer }}
    {{> scripts }}
  </body> 
</html>
