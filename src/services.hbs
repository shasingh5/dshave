<!doctype html>
<html lang="en">
  <head>
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        Welcome to D-Shave
      {{/if}}
    </title>
    {{> styles}}
  </head>
  <body class="services">
    {{> nav }}
    <div class="container-fluid">
      <div class="row row-no-padding">
        <div class="col-sm-12">
          <div class="barber-header">
            <img src="assets/img/services-header.jpg" alt="Barber Shop">
            <div class="barher-inner">
              <h1>services</h1>              
            </div>
          </div>
        </div>
      </div>
    </div> 
    
    {{> offer }}
    {{> footer }}
    {{> scripts }}
  </body> 
</html>
